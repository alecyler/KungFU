{
  "name": "idlefist",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "build:cloudflare": "vite build"
  },
  "devDependencies": {
    "vite": "latest"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
